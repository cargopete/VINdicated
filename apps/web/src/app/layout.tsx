import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "VINdicate — Free European Vehicle History",
  description:
    "Instant, free vehicle history reports for cars across Europe. Mileage history, MOT/inspection records, recalls and more — no subscription required.",
  openGraph: {
    title: "VINdicate",
    description: "Free European vehicle history reports.",
    url: "https://vindicate-app.com",
    siteName: "VINdicate",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
